// No privileged APIs exposed yet — this is where a typed, minimal contextBridge
// surface goes once the renderer needs to talk to packages/core running in the main
// process. See docs/modules/desktop.md.
export {};
